# Streamlit secrets（含 GitHub Token，絕對不能 commit）
.streamlit/secrets.toml

# Claude Code 內部資料
.claude/

# Python
__pycache__/
*.pyc
*.pyo
.env

# macOS
.DS_Store

# Write token must NEVER be committed (user enters it in app settings)
# js/config.js only stores READ_TOKEN (safe to commit)
# WRITE_TOKEN lives only in localStorage
